> **SUSPECT — planner-demonstration task (2026-08-09).** Scores next-action
> prediction on optimal-planner demonstrations. That family of task is
> self-predictable from the action stream alone: on hier-goal an n-gram scored
> 0.969 at order 1 and 0.971 at order 3, and every model there collapsed to
> ~0.02 closed-loop against a 0.010 random floor. This file has NOT been
> re-validated with an action-only n-gram control at orders 1-5, which is the
> check that would settle it.

> **INVALID — lm200 (2026-08-09).** Every result in this file was trained on
> landmark (lm200) checkpoints that never converged (final CE ~1.0 instead of
> ~0.005). The reported ranking tracks training convergence, not architecture.
> See the RETRACTION section of `CLAUDE.md` and `CORRECTED_LM200_LEADERBOARD.md`.
> Corrected ranking (fresh, current code): Level15 0.996 > TEMFaithful 0.982 >
> NoDrop 0.915 > Level15EM 0.860 > Vanilla 0.835. Clean and noise results
> elsewhere are unaffected — they retrain bit-identically
> (`NOISE_CLEAN_REVALIDATION.md`).

# Active-inference probe: is the cognitive map usable for goal-directed action?

Frozen single-env lm200 checkpoints (random-walk training, NO goal
conditioning, NO policy training). At each navigate step, for each
candidate action, score by max log p(goal_token) over a `horizon`-step
rollout. Pick argmax. Closed-loop execution — model selects action,
env executes, next obs feeds back.

Setup: T_explore=64 random walk → T_navigate=32 active-inference. Goal
= random landmark. 50 episodes/variant. Single seed (s0).

**Theoretical caveat:** horizon=1 is structurally limited — the model
predicts only the *next* observation, so log p(goal_token) is only
informative when adjacent to the goal. Multi-step horizons (4, 8) use
Dreamer-style argmax-obs rollouts to extrapolate further.

## Closed-loop success rate (50 eps, s0)

| Variant | horizon=1 | horizon=4 | horizon=8 |
|---|---|---|---|
| **RoPE** | 0.00 | 0.00 | 0.00 |
| **Vanilla** | 0.00 | 0.02 (2.5× opt) | 0.00 |
| **Level15** | 0.02 (1.5× opt) | 0.02 (2.8× opt) | 0.00 |
| **TEMFaithful** | 0.02 (3.5× opt) | 0.00 | 0.00 |

*Auto-generated by run_planning_and_scale_controls.sh*
