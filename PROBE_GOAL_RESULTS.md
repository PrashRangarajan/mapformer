# Linear probe of frozen prediction-trained models for goal-directed info

Setup: take each prediction-trained lm200 checkpoint (seed 0). Freeze it.
Capture pre-out_proj hidden state at navigate-phase action-target positions
on goal-directed episodes (random-walk explore → BFS to goal landmark).
Train a single Linear(d_model→4) head to predict the BFS-optimal action.
Evaluate on held-out env (different obs_map). Chance = 0.25.

This isolates *what the cognitive map encodes about goal-directed action*
WITHOUT any goal-directed training of the backbone. Compare across variants
to test whether correction's gain in BC fine-tuning reflects richer underlying
representation, or merely better learnability.

| Variant | Train-probe acc | Held-out probe acc |
|---|---|---|
| **Vanilla** | 0.592 | 0.555 |
| **Level15** | 0.634 | 0.630 |
| **Level15EM** | 0.649 | 0.631 |
| **Level15NoDrop** | 0.640 | 0.637 |

## Interpretation

- Held-out probe acc near 0.25: the cognitive map carries no goal-directed
  info; the BC win was pure learnability (the body adapts).
- Held-out probe acc well above 0.25, ordered like BC results: the map
  ITSELF encodes goal-relevant info; correction enriches the representation.
- TEMFaithful for reference (post-fix lm200 leader on prediction).

*Auto-generated by probe_goal_linear.py*
