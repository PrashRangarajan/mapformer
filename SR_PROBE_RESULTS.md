> **SUSPECT — planner-demonstration task (2026-08-09).** Scores next-action
> prediction on optimal-planner demonstrations. That family of task is
> self-predictable from the action stream alone: on hier-goal an n-gram scored
> 0.969 at order 1 and 0.971 at order 3, and every model there collapsed to
> ~0.02 closed-loop against a 0.010 random floor. This file has NOT been
> re-validated with an action-only n-gram control at orders 1-5, which is the
> check that would settle it.

> **INVALID — lm200 (2026-08-09).** Every result in this file was trained on
> landmark (lm200) checkpoints that never converged (final CE ~1.0 instead of
> ~0.005). The reported ranking tracks training convergence, not architecture.
> See the RETRACTION section of `CLAUDE.md` and `CORRECTED_LM200_LEADERBOARD.md`.
> Corrected ranking (fresh, current code): Level15 0.996 > TEMFaithful 0.982 >
> NoDrop 0.915 > Level15EM 0.860 > Vanilla 0.835. Clean and noise results
> elsewhere are unaffected — they retrain bit-identically
> (`NOISE_CLEAN_REVALIDATION.md`).

# Successor representation (SR) readout — reward-conditional planning from frozen models

Take prediction-trained lm200 checkpoints. Freeze the backbone. Train a
linear head to predict discounted future-cell occupancy (the SR) from the
model's hidden state. At test time, plant a one-hot reward at a random cell,
compute V = SR · r, derive greedy policy via one-step lookahead, roll out.

**This is the strongest cognitive-map content claim possible:** the prediction-
trained representation supports navigation to ARBITRARY rewards without any
goal-directed training of the backbone.

Settings: γ=0.92, future=20 steps, 20 MC samples per state.
Eval: 100 episodes, T_explore=64, T_navigate=128.
Chance success ≈ 0.0002 (random cell, random walk → goal).

## Closed-loop SR-planning success rate

| Variant | success rate (mean ± std) | head-train MSE | n_seeds |
|---|---|---|---|
| **RoPE** | 0.000 ± 0.000 | 0.00141 | 3 |
| **Vanilla** | 0.000 ± 0.000 | 0.00138 | 3 |
| **Level15** | 0.000 ± 0.000 | 0.00148 | 3 |
| **Level15GSF_NoDrop** | 0.003 ± 0.005 | 0.00157 | 3 |

(TEMFaithful skipped: out_proj is called per-step inside its forward loop,
so a single forward hook can't capture per-position hiddens cleanly. Would
require a custom extraction. Defer for now.)

## Interpretation

- Chance ≈ 0 (random walks rarely hit a specific cell within T_navigate)
- If RoPE near chance: the representation has no useful position-encoding for SR readout
- If Vanilla < Level15*: same content gap as in the linear-probe-for-action result
- If Level15* matches/beats TEMFaithful: cognitive-map content is sufficient for reward-conditional planning

*Auto-generated by probe_sr.py*
