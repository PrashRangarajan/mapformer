# Capacity control: is the Level15-over-Vanilla win architecture or parameters?

> **⚠ SUPERSEDED — read [`CAPACITY_PERREGIME.md`](CAPACITY_PERREGIME.md) instead.**
>
> This file's "CAPACITY" verdict tested **only lm200 at T=512** and is an
> lm200-specific artifact. The per-regime + length-sweep control (clean /
> noise / lm200 at T ∈ {512, 1024, 2048}) overturns it:
>
> - **clean: ARCHITECTURE** — `Vanilla_ExtraHead` (0.742) is *worse* than
>   Vanilla (0.918), nowhere near Level15 (0.995).
> - **noise: ARCHITECTURE** — `Vanilla_ExtraHead` ≡ Vanilla, doesn't reach Level15.
> - **lm200: CAPACITY** — only here does the extra head match Level15, and
>   the mechanism is "extra *content* head exploits unique landmark tokens",
>   not generic parameter count (±0.139 variance).
> - **Length sweep**: Level15's advantage *widens* with T (clean +8pp at
>   T=512 → +26pp at T=2048). Capacity slows nothing.
> - **NumberLine**: `Vanilla_ExtraHead` (0.662) ≈ Vanilla, does not close
>   Level15's +21pp arithmetic-extrapolation gap.
> - **Calibration (NLL)**: Level15 is 5–50× better than both at every
>   regime/length; the extra head actively *worsens* NLL.
>
> Honest verdict: **ARCHITECTURE on clean / noise / length / arithmetic /
> calibration**; lm200 is a regime where a generic content-retrieval head
> happens to find unique tokens to grab.

Level15 has ~305K params vs Vanilla's ~256K (+19%). `Vanilla_ExtraHead`
= Vanilla + a generic extra attention head (322K params, MORE than
Level15) — usable capacity. `L15_NoCorr` = Level15 architecture (305K)
with the InEKF correction zeroed (params present, unused).

Single-env lm200, n=3 seeds, eval_single_env.

| Variant | params | T=128 acc | T=512 OOD acc | T=512 OOD NLL |
|---|---|---|---|---|
| **Vanilla** | 255,773 | 0.844 ± 0.023 (n=3) | 0.721 ± 0.054 (n=3) | 1.342 ± 0.279 (n=3) |
| **Vanilla_ExtraHead** | 322,077 | 0.996 ± 0.005 (n=3) | 0.875 ± 0.139 (n=3) | 0.998 ± 1.174 (n=3) |
| **L15_NoCorr** | 305,373 | 0.819 ± 0.060 (n=3) | 0.677 ± 0.001 (n=3) | 1.658 ± 0.120 (n=3) |
| **Level15** | 305,373 | 0.912 ± 0.008 (n=3) | 0.828 ± 0.023 (n=3) | 0.826 ± 0.035 (n=3) |

## Per-seed T=512 OOD

| Variant | seed 0 | seed 1 | seed 2 |
|---|---|---|---|
| **Vanilla** | 0.704 | 0.793 | 0.665 |
| **Vanilla_ExtraHead** | 0.948 | 0.995 | 0.680 |
| **L15_NoCorr** | 0.677 | 0.676 | 0.679 |
| **Level15** | 0.797 | 0.834 | 0.853 |

## Decision (lm200-only — superseded)

The auto-generated verdict here said **CAPACITY** because `Vanilla_ExtraHead`
(0.875) clears Level15 (0.828) on this single lm200 point. That conclusion
**does not generalise**: on clean, noise, length-extrapolation, NumberLine,
and calibration, the extra head matches Vanilla or is worse. The
lm200-specific win is a content-channel effect (unique landmark tokens), not
generic capacity. See `CAPACITY_PERREGIME.md` for the per-regime + length
sweep that established the architectural reading.

*Auto-generated by run_capacity_rerun.sh (n=3 after GPU-1-OOM crashed-seed rerun).*
